spring:
  datasource:
    url: jdbc:mysql://localhost:3306/portafolio?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC
  thymeleaf:
    cache: false   # dev
  flyway:
    enabled: true
    locations: classpath:db/migration
server:
  port: 8080
